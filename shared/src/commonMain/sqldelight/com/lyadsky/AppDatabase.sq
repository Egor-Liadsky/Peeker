CREATE TABLE Market (
    id INTEGER NOT NULL,
    code TEXT NOT NULL,
    name TEXT NOT NULL,
    icon TEXT NOT NULL
);

insertMarket:
INSERT INTO Market (id, code, name, icon)
VALUES (?, ?, ?, ?);

getMarkets:
SELECT * FROM Market;

deleteMarkets:
DELETE FROM Market;
